schema_version: 1

context:
  name: ml-wrappers
  version: "0.6.2"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/ml_wrappers-${{ version }}.tar.gz
  sha256: 9efe7c725a6112b78906ad4ec8af60cab3f7d35fa8cfa2f20d391a943b6151d2

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
  run:
    - numpy
    - pandas
    - python >=${{ python_min }}
    - scikit-learn
    - scipy
    - packaging
tests:
  - python:
      imports:
        - ml_wrappers
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - pip check

about:
  summary: Machine Learning Wrappers SDK for Python
  license: MIT
  license_file: LICENSE.txt
  homepage: https://github.com/microsoft/ml-wrappers

extra:
  recipe-maintainers:
    - janjagusch
    - bgalvao
